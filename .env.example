# Server-only values. Never expose these in browser code or commit real values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
BLOB_READ_WRITE_TOKEN=
# Two people, seeded into the account on first read. Each value is
# login:password:display name.
MERIDIAN_OPERATOR=
MERIDIAN_CLIENT=
